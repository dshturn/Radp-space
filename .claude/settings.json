{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "git add -u && if ! git diff --cached --quiet; then git commit -m \"auto: save changes\" && git push -u origin HEAD; fi",
            "timeout": 30
          }
        ]
      }
    ]
  }
}
